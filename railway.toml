[build]
builder = "nixpacks"
buildCommand = "cd client && npm install && npm run build && cd ../server && npm install"

[deploy]
startCommand = "cd server && npx prisma db push --accept-data-loss && npm run start"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
